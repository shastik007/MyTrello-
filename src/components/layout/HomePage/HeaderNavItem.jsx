import styled from 'styled-components'

const HeaderNavItemStyles = styled.div`
`

const HeaderNavItem = (props) => {
    return(
        <HeaderNavItemStyles>
            <h4>{}</h4>
        </HeaderNavItemStyles>
    )
}